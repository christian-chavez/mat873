\usepackage[T1]{fontenc}
\usepackage[french]{babel}
\usepackage{geometry}
\usepackage{
	xcolor,
	amsmath,
	amsthm,
	amsfonts,
	amssymb,
    mathrsfs,
	enumitem,
    mlmodern,
    indentfirst,
    graphicx,
    transparent,
    pdfpages,
    xifthen,
    float,
    import,
    csquotes,
    tikz-cd,
    mathtools,
    adjustbox,
} 
\geometry{margin=1.25in}

\newcommand{\red}[1]{\textcolor{red}{#1}}

\theoremstyle{definition}
\newtheorem{thm}{Théorème}
\newtheorem{lm}{Lemme}
\newtheorem{df}{Définition}
\newtheorem{prob}{Problème}
\newtheorem{ex}{Exemple}
\newtheorem{rmk}{Remarque}

\newtheorem{cis}{Exercice}[section] 



% shorcuts
\newcommand{\incsvg}[2]{%
    \def\svgscale{#1}
    \graphicspath{{images/}}
    \input{images/#2.pdf_tex}
}
\newcommand{\Z}{\mathbb{Z}}
\newcommand{\R}{\mathbb{R}}
\newcommand{\F}{\mathbb{F}}
\newcommand{\p}{\partial}
\DeclareMathOperator{\conv}{conv}
\DeclareMathOperator{\bd}{bd}
\DeclareMathOperator{\spn}{span}
\DeclareMathOperator{\im}{Im}
\DeclareMathOperator{\Ker}{Ker}

% remove indentation at the begining of theorems 
% \makeatletter
% \let\sv@thm\@thm
% \def\@thm{\let\indent\relax\sv@thm}
% \makeatother

\usepackage[
    style=numeric, 
    sorting=nyt,
    backend=biber,
]{biblatex}
\addbibresource{biblio.bib}
\usepackage[bookmarks,hidelinks]{hyperref}

\usetikzlibrary{arrows.meta}
\usetikzlibrary{fit,backgrounds}

\tikzcdset{
  arrow style=tikz,
  line join=round,
  diagrams={
    % >={Latex[length=1mm,width=1mm]}
    % >={Triangle[length=2.6mm,width=2.2mm]},
    >={stealth[round]},
    % >={Kite[round]},
  }
} 

\tikzcdset{
  zebraA/.style={fill=gray!15, inner sep=2pt, rounded corners=2pt},
  zebraB/.style={fill=gray!10, inner sep=2pt, rounded corners=2pt}
}


\usepackage{myto}

% bordermatrix square
\makeatletter
\def\sbordermatrix#1{\begingroup \m@th
  \@tempdima 4.75\p@
  \setbox\z@\vbox{%
    \def\cr{\crcr\noalign{\kern2\p@\global\let\cr\endline}}%
    \ialign{$##$\hfil\kern2\p@\kern\@tempdima&\thinspace\hfil$##$\hfil
      &&\quad\hfil$##$\hfil\crcr
      \omit\strut\hfil\crcr\noalign{\kern-\baselineskip}%
      #1\crcr\omit\strut\cr}}%
  \setbox\tw@\vbox{\unvcopy\z@\global\setbox\@ne\lastbox}%
  \setbox\tw@\hbox{\unhbox\@ne\unskip\global\setbox\@ne\lastbox}%
  \setbox\tw@\hbox{$\kern\wd\@ne\kern-\@tempdima\left[\kern-\wd\@ne
    \global\setbox\@ne\vbox{\box\@ne\kern2\p@}%
    \vcenter{\kern-\ht\@ne\unvbox\z@\kern-\baselineskip}\,\right]$}%
  \null\;\vbox{\kern\ht\@ne\box\tw@}\endgroup}
\makeatother

% Source - https://tex.stackexchange.com/a/333383
% Posted by lecarrera, modified by community. See post 'Timeline' for change history
% Retrieved 2026-02-04, License - CC BY-SA 3.0

\makeatletter
\renewcommand*\env@matrix[1][*\c@MaxMatrixCols c]{%
  \hskip -\arraycolsep
  \let\@ifnextchar\new@ifnextchar
  \array{#1}}
\makeatother
